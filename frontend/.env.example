# Leave unset for local dev — Vite proxies /api → http://localhost:8000
#
# Production (Render static site): set your FastAPI URL with NO trailing slash, then redeploy so Vite picks it up at build time.
# VITE_API_URL=https://aggrow-web-order.onrender.com
